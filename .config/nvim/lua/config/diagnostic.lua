vim.diagnostic.config({
  update_in_insert = true, -- Update and display diagnostics while in insert mode (instead of waiting until you leave insert mode)
  virtual_text = true      -- Show inline diagnostic messages as virtual text next to the code
})